import {View} from 'react-native';
import {AppColors} from '../../../config/colors';

const InstaLine = () => {
  return (
    <View
      style={{
        width: '100%',
        height: 2,
        backgroundColor: AppColors.black_11,
        marginVertical: 16,
      }}
    />
  );
};

export default InstaLine;
